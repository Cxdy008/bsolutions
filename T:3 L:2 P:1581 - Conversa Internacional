using System; 

class URI {

    static void Main(string[] args) { 

        int N = int.Parse(Console.ReadLine());
        for(int i = 0;i<N;i++){
            int numDeFalantes = int.Parse(Console.ReadLine()), todas = 0;
            string [] linguaNativa = new string[numDeFalantes];
            for(int a = 0; a<linguaNativa.Length;a++){
                linguaNativa[a] = Console.ReadLine();
            }
            for(int a = 0; a<linguaNativa.Length;a++){
                for(int b = 0; b<linguaNativa.Length;b++){
                    if(linguaNativa[a]==linguaNativa[b]){
                        todas++;
                    }   
                }
                if(todas==linguaNativa.Length){
                    break;
                }
                else{
                    todas=0;
                }
            }
            if(todas==linguaNativa.Length){
                Console.WriteLine(linguaNativa[0]);
                
            }
            else{
                Console.WriteLine("ingles");
            }
        }

    }

}
